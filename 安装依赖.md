# 安装依赖说明

## 🔧 缺少依赖包的问题

如果运行时出现 `ModuleNotFoundError: No module named 'yaml'` 等错误，说明缺少必要的依赖包。

## 📦 安装方式

### 方式1：使用批处理文件（推荐）

双击运行 `install.bat` 文件，会自动安装所有依赖。

### 方式2：命令行安装

#### 方法A：安装所有依赖（推荐）
```bash
cd crypto-trading-system
pip install -r requirements.txt
```

#### 方法B：仅安装必需依赖
```bash
# 基础依赖
pip install PyYAML python-dotenv cryptography

# HTTP客户端
pip install requests httpx

# 数据处理
pip install pandas numpy

# 异步库
pip install aiohttp websockets

# ORM和验证
pip install sqlalchemy pydantic

# 工具库
pip install python-dateutil
```

## ⚠️ 可选依赖

以下依赖是可选的，如果安装失败可以忽略：

- `ta-lib` - 技术指标库（如果安装失败，可以手动编译或使用其他技术指标）
- `psycopg2-binary` - PostgreSQL驱动（如果不使用PostgreSQL数据库）
- `influxdb-client` - InfluxDB驱动（如果不使用InfluxDB）
- `redis` - Redis驱动（如果不使用Redis）
- `loguru` - 日志库（可以使用标准库logging替代）
- `pytest` - 测试框架（仅开发时需要）

## ✅ 验证安装

安装完成后，运行配置检查脚本验证：

```bash
python scripts/check_config.py
```

或双击 `check_config.bat`

如果能够正常运行，说明依赖安装成功！

## 🐛 常见问题

### Q1: pip不是内部或外部命令
**A**: 需要安装Python并将Python添加到系统PATH，或者使用 `python -m pip` 代替 `pip`

### Q2: 安装ta-lib失败
**A**: ta-lib需要编译，如果安装失败可以忽略，系统会使用其他技术指标计算方式

### Q3: 安装数据库驱动失败
**A**: 如果不需要数据库功能，可以忽略这些驱动的安装

### Q4: 权限错误
**A**: 尝试使用管理员权限运行，或使用 `--user` 参数：
```bash
pip install --user PyYAML python-dotenv
```

---

## 📝 最小依赖列表

如果只想快速运行系统测试，最少需要安装以下依赖：

```bash
pip install PyYAML python-dotenv cryptography requests pandas numpy
```

这足以运行配置检查和基本功能测试。

