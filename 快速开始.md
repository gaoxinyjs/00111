# 快速开始指南

## ✅ 已完成的工作

### 1. 项目结构
- ✓ 完整的目录结构已创建
- ✓ 所有模块的 `__init__.py` 文件已创建

### 2. 配置文件
- ✓ `config/config.yaml` - 主配置文件
- ✓ `config/trading_config.yaml` - 交易策略配置
- ✓ `config/risk_config.yaml` - 风险管理配置
- ✓ `config/api_config.yaml` - API接口配置

### 3. 核心模块 (src/core/)
- ✓ `config_manager.py` - 配置管理器
- ✓ `logger.py` - 日志管理器
- ✓ `exception.py` - 自定义异常类
- ✓ `security.py` - 安全管理器（API密钥管理、加密）

### 4. 数据采集模块 (src/data/)
- ✓ `okx_client.py` - OKX API客户端
  - 支持行情查询、订单簿、K线、余额、持仓查询
  - 支持下单、撤单、订单查询
  - API限流和重试机制

### 5. 分析模块 (src/analysis/)
- ✓ `deepseek_client.py` - DeepSeek API客户端
  - 市场分析功能
  - 信号生成功能
  - 风险评估功能

### 6. 主程序
- ✓ `src/main.py` - 主程序入口，包含系统初始化

## 🚀 开始使用

### 1. 安装依赖

```bash
cd crypto-trading-system
pip install -r requirements.txt
```

### 2. 配置环境变量

复制环境变量示例文件：
```bash
cp .env.example .env
```

编辑 `.env` 文件，填入你的API密钥：
```env
# OKX API配置
OKX_API_KEY=your_okx_api_key
OKX_SECRET_KEY=your_okx_secret_key
OKX_PASSPHRASE=your_okx_passphrase

# DeepSeek API配置
DEEPSEEK_API_KEY=your_deepseek_api_key

# 数据库配置（如果需要）
DB_PASSWORD=your_db_password
INFLUX_USER=your_influx_user
INFLUX_PASSWORD=your_influx_password
REDIS_PASSWORD=your_redis_password
```

### 3. 运行系统

```bash
python src/main.py
```

系统将：
1. 加载配置文件
2. 检查API配置
3. 初始化日志系统
4. 显示系统信息

### 4. 测试模块

#### 测试配置管理器
```python
from src.core.config_manager import get_config_manager

config = get_config_manager()
print(config.get_config('main', 'system.name'))
```

#### 测试OKX客户端
```python
from src.data.okx_client import OKXClient

client = OKXClient()
ticker = client.get_ticker("BTC-USDT")
print(ticker)
```

#### 测试DeepSeek客户端
```python
from src.analysis.deepseek_client import DeepSeekClient

client = DeepSeekClient()
market_data = {
    'price': 50000,
    'change_24h': 2.5,
    # ... 其他数据
}
result = client.analyze_market(market_data)
print(result)
```

## 📋 待实现的功能

### 高优先级
- [ ] 数据采集模块（data_collector.py）
- [ ] 信号生成器（signal_generator.py）
- [ ] 决策引擎（decision_engine.py）
- [ ] 交易执行模块（order_manager.py, execution_engine.py）
- [ ] 风险管理模块（risk_manager.py）

### 中优先级
- [ ] 收益统计模块（profit_statistics.py）
- [ ] 监控面板（dashboard.py）
- [ ] 数据存储模块（database.py, models.py）

### 低优先级
- [ ] Web界面
- [ ] REST API接口
- [ ] 回测模块
- [ ] 模拟交易

## 🔧 配置说明

### 主配置文件 (config/config.yaml)
- 系统配置：名称、版本、环境
- 日志配置：级别、文件路径、轮转设置
- 数据库配置：PostgreSQL、InfluxDB、Redis
- 任务调度配置：数据采集、信号生成等间隔

### 交易配置 (config/trading_config.yaml)
- 交易对配置
- 信号生成配置（技术面、资金面、链上、情绪面、AI分析）
- 信号评分配置

### 风险配置 (config/risk_config.yaml)
- 仓位管理配置
- 止损配置
- VaR配置
- 风险限制

### API配置 (config/api_config.yaml)
- OKX API配置
- DeepSeek API配置
- 限流和重试配置

## 📝 开发建议

1. **先测试核心模块**：确保配置管理、日志、安全模块正常工作
2. **逐步实现功能**：按优先级实现各个模块
3. **编写单元测试**：为每个模块编写测试
4. **使用模拟数据**：在开发时使用模拟数据，避免频繁调用API
5. **遵循日志规范**：使用统一的日志记录器记录重要操作

## 🐛 常见问题

### Q: 配置加载失败
A: 检查配置文件路径是否正确，确保 `config` 目录存在

### Q: API密钥未配置
A: 检查 `.env` 文件是否存在，环境变量是否正确设置

### Q: 模块导入失败
A: 确保在项目根目录运行，或使用 `python -m src.main` 方式运行

## 📚 文档

- [项目架构设计](../项目架构设计.md)
- [交易策略](../交易策略.md)
- [项目结构说明](./项目结构说明.md)

