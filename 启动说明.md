# 启动系统说明

## 🚀 启动方式

### 方式1：使用Python脚本（推荐）

在项目目录下运行：

```bash
cd crypto-trading-system
python start.py
```

或直接运行主程序：

```bash
python src/main.py
```

### 方式2：使用批处理文件（Windows）

双击运行 `start.bat`

## 📋 启动前检查

在启动系统前，请确保：

1. **✅ .env 文件已创建**
   ```bash
   python create_env.py
   ```

2. **✅ 依赖包已安装**
   ```bash
   pip install -r requirements.txt
   ```
   或仅安装必需依赖：
   ```bash
   pip install PyYAML python-dotenv cryptography requests pandas numpy aiohttp
   ```

3. **✅ 配置已验证**
   ```bash
   python 验证.env文件.py
   ```

## 🎯 系统启动流程

系统启动后会：

1. **加载配置**
   - 加载主配置、交易配置、风险配置、API配置

2. **初始化模块**
   - 配置管理器
   - 日志管理器
   - 安全管理器
   - 数据采集器
   - 信号生成器
   - 决策引擎
   - 交易引擎
   - 风险管理器

3. **启动数据采集**
   - 开始定时采集行情、订单簿、K线数据

4. **启动交易循环**
   - 每5分钟执行一次：
     - 收集市场数据
     - 生成交易信号
     - 生成交易决策
     - 执行交易
     - 更新持仓
     - 风险监控
     - 收益统计

## ⚠️ 重要提示

1. **首次运行建议**：
   - 先在测试环境验证
   - 从小额开始测试
   - 密切观察日志输出

2. **停止系统**：
   - 按 `Ctrl+C` 可以安全停止系统
   - 系统会自动取消所有未完成订单

3. **查看日志**：
   - 交易日志：`logs/trading/trading.log`
   - 系统日志：`logs/system/system.log`
   - 错误日志：`logs/error/error.log`

4. **监控系统**：
   - 密切关注系统日志
   - 检查交易状态
   - 监控风险指标

## 🔧 故障排除

### 如果启动失败

1. **检查依赖**：确保所有必需依赖已安装
2. **检查配置**：确保.env文件和配置文件正确
3. **检查API密钥**：确保API密钥有效且有足够权限
4. **查看错误日志**：检查 `logs/error/error.log` 了解详细错误

---

现在可以运行：

```bash
python start.py
```

或

```bash
python src/main.py
```

祝交易顺利！🎉

