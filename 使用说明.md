# 量化交易系统 - 使用说明

## 🚀 快速开始

### 1. 安装依赖

```bash
cd crypto-trading-system
pip install -r requirements.txt
```

或仅安装必需依赖：

```bash
pip install PyYAML python-dotenv cryptography requests pandas numpy aiohttp
```

### 2. 创建 .env 文件

```bash
python create_env.py
```

### 3. 验证配置

```bash
python scripts/check_config.py
```

### 4. 启动系统

**推荐方式（带诊断）：**
```bash
python run.py
```

或直接运行主程序：
```bash
python src/main.py
```

> **提示**：`run.py` 脚本会在启动前自动检查依赖、配置文件和环境，并提供详细的诊断信息。

---

## 📋 常用命令

### 创建环境配置文件

```bash
python create_env.py
```

在项目根目录创建 `.env` 文件，包含OKX API密钥配置。

### 检查配置

```bash
python scripts/check_config.py
```

验证配置文件和环境变量是否正确。

### 初始化数据库（可选）

```bash
python scripts/init_db.py
```

创建PostgreSQL数据库表结构（如果使用数据库）。

### 启动系统

```bash
python start.py
```

启动量化交易系统。

---

## ⚙️ 配置说明

### .env 文件配置

在项目根目录创建 `.env` 文件：

```env
# OKX API配置
OKX_API_KEY=your_okx_api_key
OKX_SECRET_KEY=your_okx_secret_key
OKX_PASSPHRASE=your_okx_passphrase

# DeepSeek API配置（可选）
DEEPSEEK_API_KEY=your_deepseek_api_key
```

### 配置文件

所有配置文件位于 `config/` 目录：

- `config.yaml` - 主配置（系统、日志、数据库、调度）
- `trading_config.yaml` - 交易策略配置
- `risk_config.yaml` - 风险管理配置
- `api_config.yaml` - API接口配置

---

## 🎯 系统运行流程

系统启动后会：

1. **加载配置** - 读取所有配置文件
2. **初始化模块** - 初始化各个功能模块
3. **启动数据采集** - 后台定时采集市场数据
4. **启动交易循环** - 每5分钟执行一次：
   - 收集市场数据
   - 生成交易信号（技术面、资金面、AI分析）
   - 过滤信号（假信号识别）
   - 生成交易决策（综合信号、仓位计算、风险评估）
   - 风险检查（交易前风险检查）
   - 执行交易（订单管理、执行优化）
   - 更新持仓（实时跟踪）
   - 风险监控（实时风险监控、止损检查）
   - 收益统计（计算交易收益、更新统计数据）

---

## 📊 日志查看

系统日志保存在 `logs/` 目录：

- `logs/trading/trading.log` - 交易日志
- `logs/system/system.log` - 系统日志
- `logs/error/error.log` - 错误日志
- `logs/audit/` - 审计日志

---

## ⚠️ 重要提示

1. **测试环境**：建议先在测试环境验证所有功能
2. **API密钥**：确保API密钥正确配置且权限足够
3. **风险控制**：系统有完善的风险控制，但实际交易需谨慎
4. **资金管理**：建议从小额开始测试
5. **监控日志**：密切关注系统日志和交易状态

---

## 🛑 停止系统

按 `Ctrl+C` 可以安全停止系统。系统会自动：
- 取消所有未完成订单
- 保存当前状态
- 关闭所有连接

---

## 🔧 故障排除

### 如果运行 python3 命令失败

在Windows上，通常应该使用 `python` 而不是 `python3`：
```bash
python src/main.py
```

### 依赖包未安装

```bash
pip install -r requirements.txt
```

或仅安装必需依赖：
```bash
pip install PyYAML python-dotenv cryptography requests pandas numpy aiohttp
```

### 密钥文件创建失败

如果遇到密钥文件创建错误（如 `系统找不到指定的文件。: 'C:\\Users\\...\\.trading_system_key'`），请检查：

1. 用户主目录是否存在且有写入权限
2. 防病毒软件是否阻止了文件创建
3. 运行诊断脚本：`python run.py` 查看详细信息

### 配置文件错误

运行配置检查：
```bash
python scripts/check_config.py
```

### API密钥错误

检查 `.env` 文件中的API密钥配置：
```bash
python create_env.py
```

### 查看错误日志

检查以下日志文件了解详细错误信息：
- `logs/error/error.log` - 错误日志
- `logs/system/system.log` - 系统日志
- `logs/trading/trading.log` - 交易日志

### 使用诊断脚本

运行诊断脚本可以自动检查常见问题：
```bash
python run.py
```

---

## 📚 相关文档

- [README.md](./README.md) - 项目说明
- [启动说明.md](./启动说明.md) - 启动详细说明
- [项目架构设计.md](../项目架构设计.md) - 架构设计

---

祝交易顺利！🎉
